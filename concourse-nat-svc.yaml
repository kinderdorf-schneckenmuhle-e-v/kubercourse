apiVersion: v1
kind: Service
metadata:
  name: concourse-nat-service
spec:
  type: NodePort
  selector:
    app: concourse-web
  ports:
  - protocol: TCP
    name: "concourse-nat"
    port: 8080
    targetPort: 8080
  externalIPs:
  - 127.0.0.1
